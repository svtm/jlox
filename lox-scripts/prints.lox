class MyClass {
    sum(a, b) {
        return a + b;
    }
}

var myInstance = MyClass();

print myInstance.sum(2, 3);

class Math {
  class square(n) {
    return n * n;
  }
}

print Math.square(3); // Prints "9".

Math.pi = 3.14;
print Math.pi;

class Circle {
    init(radius) {
        this.radius = radius;
    }

    area {
        return Math.pi * this.radius*this.radius;
    }
}

var circle = Circle(2);
print circle.area;

var a = (true) ? "true" : "false";

print a;